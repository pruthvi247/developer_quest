apiVersion: v1
kind: ReplicationController
metadata:
  name: myapp-rc
  labels:
    app: nginx
    env: production
spec:
  replicas: 3
  template:
    metadata:
      name: myapp-pod
      labels:
        app: nginx
        env: production
    spec:
      containers:
        - name: nginx-controller
          image: nginx